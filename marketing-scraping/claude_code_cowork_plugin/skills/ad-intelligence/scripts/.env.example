# Copy to `.env`, fill in, then load before running:
#   set -a; source scripts/.env; set +a
# NEVER commit the real .env (see .gitignore).

# Your own Apify account token (Apify Console → Settings → Integrations).
APIFY_TOKEN=

# No LLM API key needed. OCR of Google ad images is done by a cheap-model
# subagent via the `claude` CLI — no extra cost beyond your normal Claude usage.
